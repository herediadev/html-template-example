/**
 * {
 *     name:"ejemplo",
 *     text: "ejemplo de cualquier cosa que escribimos"
 * }
 * @param object
 */
const procesarTexto = (object) => {
    console.log(object);
};


module.exports = {
    procesarTexto
}
