/**
 * {
 *     name:"ejemplo",
 *     text: "ejemplo de cualquier cosa que escribimos"
 * }
 * @param object
 */
const processWords = (object) => {
    console.log(object);
};


module.exports = {
    processWords
}
